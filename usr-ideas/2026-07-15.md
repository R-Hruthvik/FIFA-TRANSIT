# User Ideas — 2026-07-15

## Idea 1 (raw)
I want real stadium miniature blueprint instead of square and 4 circles as stadium you made in staff page real stadium 2d blueprint will be good, i have more

## Idea 1 (refined)
Replace the abstract square-with-4-circles stadium representation in the Staff Hub heatmap with a realistic 2D stadium blueprint — an oval bowl shape with stands, field markings, and actual gate positions on the perimeter. This makes the congestion visualization immediately readable as a real venue rather than a schematic.

## Idea 2 (raw)
We did not touch user profiles admin profiles and auths, so first auth is google auth or simple email and pass auth with robust protection, and we collect username and emailfrom google, and for app's username it will not be default selected same username from google, but let user type some anonymous or any username, we will have provide user id for each user so duplicate usernames is fine, we will not show user ids at any main pages we will only show it in settings page

## Idea 2 (refined)
No user profiles, admin profiles, or auth system has been built yet. The auth layer should support two methods: Google OAuth and email/password with robust protection (hashing, rate limiting, etc.). On Google sign-in, collect email and name from Google, but don't auto-fill the app username from the Google name — let the user pick any display name (anonymous or named). Internally assign a unique user ID (UUID) to each user so duplicate display usernames are allowed. User IDs are never shown on main pages — only visible in a dedicated Settings page.

## Idea 3 (raw)
And for admin profile let admin will login with same way as any other user but with strict username and password which is unique and we can introduce many security features for admin logins like new pass for each days or complex username or 2fa and after admin logs in, we can give some developer settings to admin, also we didn't talk about staff profiles i mean admin will log in as same login page as other users but uses email and pass option but types username and password which is very secure and robust and does not use google auth

## Idea 3 (refined)
Admins use the same login page as regular users but authenticate exclusively via email/password (no Google auth). Admin credentials require strict, unique usernames and passwords with enhanced security features: daily password rotation, complex username requirements, and 2FA. After login, admins get access to a developer settings panel. Staff profiles are separate from admin — the auth system needs to support three roles: fan (Google or email/pass), staff (email/pass only), and admin (email/pass only with elevated security).

## Idea 4 (raw)
For staff profile staff can login as other users using any method but sign up or registration is very strict staff uses staff id issued by stadium department and when sign up or registration application is done it is went to waiting list to be approved by admin, admin should verify it by cross reference with any valid data on staffs

## Idea 4 (refined)
Staff registration is strict: staff must provide a staff ID issued by the stadium department to sign up. Once the registration application is submitted, it goes into a pending/waiting list. An admin must approve it by cross-referencing the provided details against valid staff records. Staff can log in using any auth method (Google or email/password) once approved. This means the auth system needs a registration flow with role selection, staff ID verification, and an admin approval queue.

## Idea 5 (raw)
the users can login directly using cookies of google like there is a popup of google to auto sign in for the app or website

## Idea 5 (refined)
Implement Google One Tap / Google Sign-In popup so users can authenticate directly via their Google session cookies without navigating to a separate login page. The popup auto-detects if the user is already signed into Google and offers a one-click sign-in experience for the app.

## Idea 6 (raw)
Curr real match cores and other data is not updated correctly i want app to correctly show match day and real scores and cards shown in fan page where gate is clear or some other cards i need them to be valid to some point like data should be come from real world or simulated one, not just placeholders

## Idea 6 (refined)
Current match scores and related data on the Fan page are not accurate. Need real match-day data displayed correctly — live scores, cards (yellow/red), and gate congestion status should all show valid information. Data should come from either a real-world API (live match feeds) or a realistic simulation engine, not static placeholder values. The Fan page cards need to reflect actual match state.

## Idea 7 (raw)
Add dark mode toggle to settings

## Idea 7 (refined)
Implement dark/light theme toggle in settings page with localStorage persistence and OS preference detection

## Idea 8 (raw)
add dark mode toggle

## Idea 8 (refined)
Implement dark/light theme toggle in settings with localStorage persistence
